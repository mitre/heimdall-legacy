--- 
before: 
  - "mkdir -p /tmp/db"
  - "/usr/bin/mongod --dbpath /tmp/db --fork --syslog"
build_dependencies: 
  - ImageMagick-devel
  - libxml2-devel
  - libxslt-devel
  - gcc
  - make
  - automake
dependencies: 
  - mongodb-org-server
  - ImageMagick
  - libxml2
  - ImageMagick-c++
  - libxslt
targets:
  centos-7:
  
