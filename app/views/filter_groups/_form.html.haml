= form_with(model: filter_group, local: true) do |form|
  - if filter_group.errors.any?
    #error_explanation
      %h2
        = pluralize(filter_group.errors.count, "error")
        prohibited this filter_group from being saved:
      %ul
        - filter_group.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name, id: :filter_group_name
  %br/
  .actions
    = button_tag(type: "submit", class: "btn btn-primary") do
      %i.fa.fa-edit
      Save
